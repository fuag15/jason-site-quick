- if user_signed_in?
  Logged in as #{ current_user.email }
  = link_to "Log Out", destroy_user_session_path, class: 'muted', method: :delete
- else
  = link_to "Sign In", new_user_session_path, class: 'muted'